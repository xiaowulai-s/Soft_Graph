perf(v2.0.0-M1): 软件发现 11.06s → 4.46s，三项性能验收全绿

第二波优化（接续 5e72835 的会话池与 walkRule 并发）：

便携扫描并发化（scanPortable / dirSize / scorePortableDir）
- worker 池拉取共享 BFS 队列（并发 8，SG_PORTABLE_CONCURRENCY 可调），
  消除串行逐目录 readPeMeta + dirSize 的等待串联
- dirSize 文件 stat 分块并行（每块 16）；目录递归保持串行（跨目录已并发）
- 体积延迟补算：评分阶段不再无条件跑最贵的 dirSize（递归 stat 最多 3000 文件），
  阈值判定用不到它 —— 命中后才对命中项并行补算，非命中目录零开销

已安装解析并发化（scanInstalled）
- 280 项 findMainExe（安装目录深度 2 遍历 + 逐 exe stat）改 mapPool 并发池
- Store 应用解析同样并发化；进度计数改按完成数推进，批次语义保留

五来源枚举拆分并行（winenum）
- CORE（卸载项/AppPaths/MSI/服务四个纯注册表来源）与 STORE（Get-AppxPackage）
  拆两个脚本，经会话池双会话 Promise.all 并行，墙钟从串行相加变取最大值
- 热会话实测：CORE 141ms · STORE 1590ms（Get-AppxPackage 为 WinRT 下限）
- 尝试过 AppModel Repository 注册表直读替代 Get-AppxPackage：数据不合格
  （468 项含 staging 残留、无 Name/Publisher、PackageRootFolder 常为盘根），放弃

验收（docs/benchmarks/baseline-2026-09-12.md 同机对照）
- 软件发现 11.06s → 4.46s（−60%）✅ ≤6s
- 全盘垃圾扫描 106.81s → 48.46s（−55%）✅ ≤100s
- 图谱缓存读取 1ms ✅；基准整体 ~180s → 69.6s
- 187 用例 / 43 套件全绿；typecheck + build 通过
